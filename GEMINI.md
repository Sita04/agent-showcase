# Workspace Mandates

## Security & System Integrity
- **NEVER** commit or stage `DEVLOG.md` or `TDD.md` files. These are for local tracking only.
- Protect all local logging files (`*.log`) and temporary session data.
